{
  "version": "0.6.22",
  "headline": "pki.acme.client gains scheduleRenewal, the RFC 9773 auto-sleeping certificate-renewal loop.",
  "summary": "The ACME client's scheduleRenewal(certDer, opts) drives the RFC 9773 sec. 4.1 renewal timer that renewalWindow deliberately omitted: it fetches the ARI decision, sleeps via the client's injectable sleeper until the sooner of the selected renewal instant and the Retry-After, and refetches until the suggested window says it is time to renew, resolving { reason: \"renew-now\", decision }. It stops at the certificate's notAfter (a client must not check RenewalInfo after expiry), on a caller shouldStop() replaced-signal, and on an optional check or total-wait budget, and it retries an invalid window or transport error on the sec. 4.3.3 schedule (a 5xx backs off exponentially, other errors wait a configurable interval defaulting to six hours). An optional renew(decision) callback is awaited at renew-now and, returning a new certificate, reschedules on it. It composes the shipped renewalWindow decision, so valid ACME flows are unchanged.",
  "sections": {
    "Added": [
      "pki.acme.client(...).scheduleRenewal(certDer, { random, shouldStop, renew, maxChecks, maxWait, longTermRetrySeconds, temporaryBaseSeconds }) is the RFC 9773 sec. 4.1 auto-sleeping renewal loop. It composes renewalWindow and the client's injectable sleep/clock: fetch the ARI decision, sleep until the sooner of the selected instant and the clamped Retry-After, refetch until renewNow, then resolve { reason: \"renew-now\", decision }. It resolves { reason: \"expired\" } once the certificate passes its notAfter (a client must not check RenewalInfo after expiry, RFC 9773 sec. 4.3), { reason: \"stopped\" } when shouldStop() signals the certificate is replaced, and { reason: \"budget\" } at the optional maxChecks or maxWait bound. An invalid suggested window or a transport error retries on the sec. 4.3.3 schedule, with a 5xx backing off exponentially from temporaryBaseSeconds and every other error waiting longTermRetrySeconds (default six hours). A supplied renew(decision) callback is awaited at renew-now instead of resolving, and returning a new certificate DER reschedules on it."
    ]
  }
}
