{
  "version": "0.6.55",
  "date": "2026-09-07",
  "headline": "A Diffie-Hellman key proves possession by agreeing a secret with the authority, without signing anything.",
  "summary": "RFC 4211 gives a key agreement key four ways to prove possession, and the fourth needs no signature: the requester and the authority agree a secret, and that secret keys a MAC over the request. pki.crmf.build assembles it, which is the last of the four it did not. Section 4.3 requires every Diffie-Hellman implementation to support this proof.",
  "sections": {
    "Added": [
      "pki.crmf.build assembles the agreeMAC proof of possession (RFC 4211 section 4.3, RFC 2875 section 3). The requester supplies pop.key, its own Diffie-Hellman private key, and pop.caCert, the authority's certificate it already holds; the two agree a secret, a key is derived from it, and that key MACs the request. The proof carries id-dhPop-static-HMAC-SHA1, and both that OID and id-alg-dhPOP are registered.",

      "The proof value is the DhPopStatic structure section 3 defines, not the bare MAC: the BIT STRING carries a SEQUENCE of the issuer name and serial number of the certificate the authority's key came from, and the HMAC result as an OCTET STRING. The section says so after the computation it describes, \"i.e. encodes the above sequence instead of the raw output from 3d\".",

      "The key is derived as RFC 2875 section 3 states it: SHA-1 over the authority certificate's subject name, the agreed secret, and its issuer name, in that order. Section 4.3 settles which certificate those names come from and what to do when one is absent, so an authority certificate whose subject name is empty contributes its subjectAltName instead, and one carrying neither is refused rather than keyed from nothing. Section 4.3 states the same fallback for an empty issuer name, which a certificate reaching this proof cannot carry: RFC 5280 section 4.1.2.4 requires a non-empty issuer, and the certificate parser enforces it.",

      "The MAC covers the DER certReq, and the request must name both a subject and a public key. Section 4.3's own ASN.1 comment states both, where the prose states neither: the proof is \"a MAC (over the DER-encoded value of the certReq parameter in CertReqMsg, which must include both subject and publicKey)\". A template naming only one of them is refused.",

      "pop.key must be the private half of certTemplate.publicKey. The authority derives its side of the agreement from the requested key, so a private key from another pair agrees a secret with the authority and yields a MAC the authority cannot reproduce: a proof of possession of something the request never asked to have certified. The encryptedKey arm holds its enclosed key to the same rule.",

      "The proof is admitted only for a finite-field Diffie-Hellman pair, which is what section 3 defines it over, and only when the requester's key can actually reach the authority's, which section 4.3 requires. An elliptic-curve or montgomery pair agrees a secret through the same operation and is refused rather than emitted under an OID naming something the recipient is not performing; so is a key on another group, and a key this runtime cannot classify is named as unrecognized rather than reported as a key type. Every copy of private material the proof allocates is cleared before the call returns, on the refusing paths as well as the building one: the agreed secret, the key derived from it, and the copy taken of the caller's key. The caller's own key is left untouched."
,
      "The authority certificate is read in both encodings a finite-field key is written in. An X.509 Diffie-Hellman certificate names dhpublicnumber and carries X9.42 DomainParameters (RFC 3279 section 2.3.3), where the runtime classifies only the PKCS#3 form, so the key is rewritten into the form it reads. DomainParameters also state q, the order of the subgroup the public value must lie in, which the PKCS#3 form cannot carry: it is checked before the parameter stating it is dropped, and a value outside that subgroup is refused rather than agreed with, since the secret it produces would expose the requester's private exponent modulo the small order it actually has. The stated q is held against p and g rather than believed, and the cofactor is held against p and q where it is given. p and q must both be prime, p because every statement the parameters make is about the group modulo a prime, and q because that is what makes the subgroup test mean anything: a composite q bounds the public value's order to a divisor of q rather than to q, and a certificate stating q = p-1 would otherwise satisfy every structural test, since g raised to p-1 is 1 for the whole group. The cofactor and the validation parameters are each optional and independent, so a fourth field is read by its tag: validation parameters are accepted with no cofactor before them, and are held to the seed BIT STRING and pgenCounter INTEGER they consist of. Every container is read for the tag it carries rather than for having children, since a context-specific constructed value in place of the DomainParameters SEQUENCE would otherwise be rewritten into a PKCS#3 key that imports while the request still carried the original the recipient's own reader refuses. The parameters are sized before anything is computed with them, since every one of these tests raises one operand to the power of another and the DER reader admits an integer far wider than any Diffie-Hellman group; the bound is the widest MODP group RFC 3526 defines, and an authority certificate exceeding it is refused on its size rather than by computing with it.",

      "The authority certificate's keyUsage is enforced. A keyUsage extension restricts the certified key to the purposes it asserts (RFC 5280 section 4.2.1.3), and this proof uses the authority's key to agree a secret, so a certificate that states its purposes without stating keyAgreement is refused rather than used for it. A certificate carrying no keyUsage states no restriction and is unaffected."
    ],
    "Fixed": [
      "pki.sigstore.verifyBundle refuses a bundle that carries both content arms. A bundle setting dsse_envelope and message_signature together was verified on the envelope alone, returning a verdict that said nothing about the other arm the bundle carried. The bundle's content is a oneof, so exactly one arm may be present and both is now sigstore/bad-bundle.",

      "The same rule holds for verificationMaterial, whose certificate, x509CertificateChain and publicKey are a oneof of the same kind. More than one arm set drew the leaf certificate from one and the chain it is validated through from another, so the certificate whose key was checked and the certificates it was checked through came from different statements about the same signer, and a publicKey beside either certificate arm named a second signer identity the verdict said nothing about."
    ]
  }
}
