{
  "version": "0.2.32",
  "headline": "X.509 certificates now decode the Microsoft Active Directory Certificate Services enrollment extensions, and pki.lint's critical-extension check is aligned with certification-path validation.",
  "summary": "The toolkit now decodes the enterprise-CA enrollment extensions Active Directory Certificate Services stamps onto issued certificates ([MS-WCCE] / [MS-CRTD]): the v2 certificate-template marker (szOID-CERTIFICATE_TEMPLATE -- the template OID and its major/minor version, each a full 32-bit value), the legacy v1 template name (szOID-ENROLL_CERTTYPE_EXTENSION, a BMPString), the CA version (szOID-CERTSRV_CA_VERSION, surfaced both as the raw value and split into the CA key index and the certificate index), the previous-CA-certificate hash (szOID-CERTSRV_PREVIOUS_CERT_HASH), and the application policies (szOID-APPLICATION_CERT_POLICIES, decoded with the same rules as RFC 5280 certificate policies). Each is decoded by the toolkit's certificate-extension decoders and rendered by pki.inspect; a malformed shape -- a non-minimal integer, a wrong string type, a trailing byte, a constructed string, a value outside the 32-bit range -- fails closed as a typed error. Because certification-path validation does not process these extensions' semantics, a critical instance is rejected there and flagged by pki.lint. This release also aligns pki.lint's unrecognized-critical-extension check with the certification-path validator: an extension the linter can decode for display but whose critical semantics the validator does not process (an authority/subject key identifier, a freshest-CRL pointer, an SCT list, a qualified-certificate statement, an enterprise-CA extension) is now reported when marked critical -- matching what path validation already rejects -- while precertificatePoison, which RFC 6962 requires to be critical, is not.",
  "sections": {
    "Added": [
      "pki.schema.x509.parse decodes the Microsoft Active Directory Certificate Services enrollment extensions ([MS-WCCE] / [MS-CRTD]): the v2 certificate template (szOID-CERTIFICATE_TEMPLATE -- template OID plus major/minor version), the legacy v1 template name (szOID-ENROLL_CERTTYPE_EXTENSION), the CA version (szOID-CERTSRV_CA_VERSION, surfaced as the raw value and as the CA key index / certificate index split), the previous-CA-certificate hash (szOID-CERTSRV_PREVIOUS_CERT_HASH), and the application policies (szOID-APPLICATION_CERT_POLICIES, decoded as RFC 5280 certificate policies). Each is rendered by pki.inspect and fails closed with a typed error on a malformed shape. The version fields accept the full 32-bit range Active Directory uses, not a signed subset."
    ],
    "Changed": [
      "pki.lint's unrecognized-critical-extension check now mirrors certification-path validation: it reports a critical extension whose semantics the path validator does not process -- an authority/subject key identifier, a freshest-CRL pointer, an SCT list, a qualified-certificate statement, or a Microsoft enterprise-CA extension -- rather than treating any extension it can merely decode as processed. precertificatePoison, which RFC 6962 requires to be critical, is not reported. A conforming relying party must reject a critical extension it cannot process, so a certificate whose critical enterprise or qualified-certificate constraints this toolkit does not enforce is now surfaced by the linter."
    ]
  }
}
