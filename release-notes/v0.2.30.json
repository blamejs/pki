{
  "version": "0.2.30",
  "headline": "C509 CBOR-encoded certificates arrive as pki.schema.c509.parse: decode the compact CBOR profile of X.509 in both its natively-signed and X.509-re-encoded forms, reconstructing the original DER byte-for-byte so the original signature still verifies.",
  "summary": "C509 (draft-ietf-cose-cbor-encoded-cert) is a compact CBOR encoding of an X.509 certificate for constrained and IoT deployments. pki.schema.c509.parse(bytes) decodes it under the strict deterministic-CBOR codec the toolkit already ships, reading the eleven-element certificate array -- the unwrapped serial number, validity instants, algorithm/attribute/extension identifiers, names (including the RFC 9542 MAC-address form), the point-compressed public key, and the keyUsage short form -- fail-closed, with a malformed shape throwing a typed error that carries the underlying CBOR or DER fault. It handles both C509 forms: the natively-signed certificate (surfacing the exact signed byte range a verifier hashes) and the CBOR re-encoding of a DER X.509 certificate, which it inverts back to the original DER byte-for-byte -- de-compressing the EC public-key point and re-emitting every field as canonical DER -- so the original certificate's signature still verifies and the result round-trips through pki.schema.x509.parse. Because it decodes CBOR rather than DER, it is reached by an explicit call and is not auto-detected by pki.schema.parse. A field outside the reconstruction's covered set fails closed rather than producing a partial certificate. This release also closes a fail-open in path validation: a trust anchor's per-purpose distrust-after date supplied as an invalid Date object is now rejected at input rather than silently disabling the distrust check.",
  "sections": {
    "Added": [
      "pki.schema.c509.parse(bytes) decodes a C509 CBOR-encoded certificate (draft-ietf-cose-cbor-encoded-cert) into structured, validated fields. It reads both certificate forms -- natively-signed (c509CertificateType 2) and the CBOR re-encoding of a DER X.509 v3 certificate (type 3) -- over the strict deterministic-CBOR codec, fail-closed. For a type-3 certificate it reconstructs the original DER byte-for-byte (de-compressing the EC point, re-emitting each field as canonical DER, re-wrapping the ECDSA signature) so the original signature verifies and the certificate round-trips through pki.schema.x509.parse; a field it cannot invert byte-exactly fails closed. It decodes CBOR, not DER, so it is an explicit-call surface and is not auto-routed by pki.schema.parse."
    ],
    "Fixed": [
      "Certification-path validation now rejects a trust anchor whose per-purpose distrust-after date is an invalid Date at input (path/bad-input). Previously a malformed date value passed the Date type check but compared as not-a-number, silently disabling the distrust-after restriction it was meant to enforce."
    ]
  }
}
